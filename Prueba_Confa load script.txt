///$tab Main
SET ThousandSep=',';
SET DecimalSep='.';
SET MoneyThousandSep=',';
SET MoneyDecimalSep='.';
SET MoneyFormat='$ ###0.00;-$ ###0.00';
SET TimeFormat='h:mm:ss TT';
SET DateFormat='M/D/YYYY';
SET TimestampFormat='M/D/YYYY h:mm:ss[.fff] TT';
SET FirstWeekDay=6;
SET BrokenWeeks=1;
SET ReferenceDay=0;
SET FirstMonthOfYear=1;
SET CollationLocale='en-US';
SET CreateSearchIndexOnReload=1;
SET MonthNames='Jan;Feb;Mar;Apr;May;Jun;Jul;Aug;Sep;Oct;Nov;Dec';
SET LongMonthNames='January;February;March;April;May;June;July;August;September;October;November;December';
SET DayNames='Mon;Tue;Wed;Thu;Fri;Sat;Sun';
SET LongDayNames='Monday;Tuesday;Wednesday;Thursday;Friday;Saturday;Sunday';
SET NumericalAbbreviation='3:k;6:M;9:G;12:T;15:P;18:E;21:Z;24:Y;-3:m;-6:μ;-9:n;-12:p;-15:f;-18:a;-21:z;-24:y';

///$tab Extracción
Extraccion:
LOAD
    NUMERO_CREDITO,
    num(CEDULA_PERSONA) 		as CEDULA_PERSONA,
    TIPO_PERSONA,
    CATEGORIA,
    date(FECHA_NACIMIENTO) 		as FECHA_NACIMIENTO,
    num(EDAD) 					as EDAD,
    RANGO_EDAD,
    GENERO,
    MUNICIPIO_SERVICIO,
    date(FECHA_SERVICIO) 		as FECHA_SERVICIO,
    AÑO_SERVICIO,
    MES_SERVICIO,
    MES_REPORTE_NUMERO,
    DIA_SERVICIO,
    DIA_SEMANA_SERVICIO,
    NUM(PERIODO) as PERIODO,
    date(FECHA_REFERENCIA) 		as FECHA_REFERENCIA,
    MUNICIPIO_VENTA,
    LUGAR,
    FORMA_PAGO,
    CUOTA,
    FORMA_DESEMBOLSO,
    PLAZO,
    EMPRESA,
    PARTICIPANTES,
    NUMERO_PERSONAS,
    MACROSERVICIO,
    SERVICIO,
    PROGRAMA,
    SUBPROGRAMA,
    ZONA_VENTA,
    ZONA_SERVICIO,
    Replace(TASA_INTERES,',','.') AS TASA_INTERES,
    GENERO_PERSONA,
    'prueba github' as prueba,
    '2025'
FROM [lib://DataFiles/Datos_Creditos.csv]
(txt, utf8, embedded labels, delimiter is ',', msq);


///$tab Transformación - Calidad
// Carga de la data extracción para generar ajustes en campos y logicas de calidad

Datos_Creditos:
Load *,
RowNo() as ID_temp,
if(len(CEDULA_PERSONA)<6,'Revisión CC',
	if(CEDULA_PERSONA<>Num(CEDULA_PERSONA),'Revisión CC','Ok')) as Flag_cc
Resident Extraccion;


Drop Table Extraccion;
// Generación de Group by y campo duplicados de credito - identificación casos atipicos

Dup:
Load
NUMERO_CREDITO,
Count(NUMERO_CREDITO) as Flag_Duplicado,
Concat(ID_temp,',') as Id_Dup
Resident Datos_Creditos
Group By NUMERO_CREDITO;

// PRUEBA DE PRONOSTICO 1 - EN PAUSA
// Temp:
// Load 
// NUMERO_CREDITO,
// // Count(NUMERO_CREDITO) as Cantidad,
// PERIODO as Periodo_proyec,
// AÑO_SERVICIO as AÑO_PROYECT
// Resident Datos_Creditos
// Where   [AÑO_SERVICIO] >= 2024
// // Group by PERIODO;
// ;

// proyeccion:
// Load 
// 2026 as AÑO_PROYECT,
// Periodo_proyec,
// Avg(NUMERO_CREDITO) as Cant_proyeccion
// Resident Temp
// Group BY Periodo_proyec;

// drop Table Temp;

// prueba2:
// Load 
// Date(MakeDate(PERIODO,1)) as PERIODO_2,
// NUMERO_CREDITO,
// [AÑO_SERVICIO]
// Resident Datos_Creditos
// Where   [AÑO_SERVICIO] >= 2024;

// Pron:
// lOAD
// PERIODO_2,
// NUMERO_CREDITO,
// fore
// Resident prueba2;

// real:
// Load
// [AÑO_SERVICIO],
// PERIODO,
// SUM(NUMERO_CREDITO) as Cantidad
// Resident Datos_Creditos
// Where [AÑO_SERVICIO] >=2024
// Group by PERIODO;

// meses2026:
// load 2026 as [AÑO_SERVICIO],
// PERIODO
// AutoGenerate 12
// wit

exit Script;